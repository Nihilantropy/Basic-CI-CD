Create an interaction between multiple branches (developer (already working) - and main (not working))
Work on the main branch
Rebuild the Docker agent for the Jenkins pipeline to save compute resourcesù

Work on Jenkins pipeline, automated debug and testing tools, gilab real-time feedback, multi-branch integration